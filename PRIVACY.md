# 隐私说明（Privacy）

蛋蛋桌宠完全在你自己的 Windows 电脑上运行。**本项目没有服务器，作者收不到你的任何数据**：没有遥测、没有崩溃上报、没有使用统计、没有账号体系、没有广告或分析 SDK。

但它**不是一个离线软件**。下面第一节是本文件里最重要的一条，请务必读完。

---

## 一、会离开你电脑的数据：全部发给「你自己配置的」模型服务商

蛋蛋的聊天、翻译、识图、干活能力都由大模型提供。你在设置窗里填的是**你自己的 API Key**，请求由你的电脑**直连**你选的服务商（DeepSeek 或 OpenAI），不经过作者或任何中间服务器。

会被发送出去的内容：

| 功能 | 发送的内容 | 触发时机 |
| --- | --- | --- |
| 聊天 | 你输入的每一句话、最近若干轮对话历史、人设提示词、长期记忆里的全部事实 | 你在聊天窗发消息时 |
| 意图路由 | 你输入的那句话（一次小请求，判断这句是闲聊还是干活） | 每条消息，自动 |
| 长期记忆抽取 | 刚才这一问一答会再发一次给模型，让它提炼「关于主人的事实」 | 每轮聊天后，后台自动 |
| 上下文压缩 | 对话估算超过 10 万 token 时，把较旧的对话原文发给模型做摘要 | 自动 |
| 划词翻译 | 你选中的那段文字 | 按下翻译快捷键时 |
| 截图识别 | 你框选的那块屏幕区域的图片（base64 内嵌在请求里） | 按下识图快捷键并框选后 |
| 干活（agent） | 你的任务描述，以及 opencode 在工作目录里为完成任务而读到的文件内容 | 触发干活任务时 |
| 拉取模型列表 | 只有一个带 Key 的 `GET /models` 请求，不含你的任何内容 | 在设置窗点刷新模型列表时 |

**结论：任何交给蛋蛋的内容，都等同于交给了你选的那家模型服务商**，其留存期限与使用方式以对方的隐私政策为准。截图识别尤其要留意——框选区域里的聊天记录、身份信息、代码、密钥都会随图片一起上传。

### 划词翻译与剪贴板

按下翻译快捷键时，程序会模拟一次 `Ctrl+C` 把当前选中的文字取到剪贴板，读取后立刻把剪贴板恢复成原来的内容。它**只在你按快捷键的那一刻读取一次**，不做后台剪贴板监听。

---

## 二、存在你电脑上的数据

| 数据 | 位置 | 说明 |
| --- | --- | --- |
| API Key、服务商、模型、工作目录、人设 | 后端目录下的 `config.yaml` | 源码运行时是 `backend/config.yaml`；安装版在安装目录的 `resources\backend\config.yaml`。**明文 YAML，未加密** |
| 长期记忆 | 后端目录下的 `memory.db`（SQLite） | 模型从对话中提炼的「关于主人的事实」，一条一行 |
| 运行日志 | `%APPDATA%\dandan-pet\dandan.log` | 启动、连接、路由、错误等；超过 1MB 时下次启动清空。可能含你的工作目录路径与错误详情 |
| 窗口状态、快捷键设置 | `%APPDATA%\dandan-pet\` 下的 `ui-state.json`、`shortcuts.json` | 不含 Key |
| opencode 的服务商配置 | `%USERPROFILE%\.config\opencode\opencode.json` | 由蛋蛋写入，**含明文 API Key**，见下文 |

**聊天记录本身不落盘**：对话历史只存在后端进程的内存里，后端一退出就没了。唯一会被持久化的是模型提炼出的那几条「事实」，它们进了 `memory.db`。

### API Key 是明文存储的（如实告知）

Key 以明文写在 `config.yaml` 里，**没有加密**，也没有存进 Windows 凭据管理器。任何能读到你用户目录的程序或人都能拿到它。这是当前实现的已知取舍，不是疏忽被隐瞒——请据此判断是否在共用电脑上使用。

### 蛋蛋会把你的 Key 同步写进 opencode 的配置文件

你在设置窗填好 Key、选好模型后，程序会把**同一份 API Key 和 base_url 同步写入干活内核 opencode 的配置文件** `%USERPROFILE%\.config\opencode\opencode.json`（写在 `provider.<服务商>.options.apiKey`），这样你只填一次就能同时用于聊天和干活。这份文件也是明文 JSON。写入时会保留该文件里你已有的其他配置，但会覆盖同名服务商的那一段。

**所以你的 Key 在本机有两份副本**，清理时两处都要处理。

---

## 三、网络行为

- 后端只监听 `127.0.0.1:8765`（HTTP `/health` + WebSocket `/ws`），**只接受本机连接**，不对局域网或互联网开放，也不做端口转发或内网穿透。
- 主动发起的外网请求只有两类：
  1. 第一节表格里那些发往**你所选服务商**的 API 调用；
  2. 干活内核 opencode 首次运行时，从 npm 源下载它自己需要的模型适配包（默认指向国内镜像 `registry.npmmirror.com`，你若自己设过 `NPM_CONFIG_REGISTRY` 则尊重你的设置）。
- 没有任何请求发往作者或第三方统计服务——本项目根本没有这样的服务。
- 不检查更新、不回传版本号。

---

## 四、本地读取与监测行为

- **截图识别**：按下识图快捷键时抓取一次当前主屏画面供你框选，整屏图片只存在内存中；只有你框选的那一块会被发给模型。不做后台连续截屏、不录屏。
- **主动提醒**：开启后会监测你在设置里指定的**工作目录（白名单沙箱）顶层**的新文件创建事件，只用到文件名，**不读取文件内容**，也不递归子目录。另外会按闲置时长（默认 15 分钟没互动）主动说句话，这一项纯靠本机计时，不监测你在用什么软件。
- **全屏检测**：会检测前台是否有全屏程序（用于自动躲开），只判断窗口状态，不记录窗口标题、不上传。
- **干活（agent）**：opencode 以你设置的工作目录为 cwd 运行，会读取该目录中与任务相关的文件，这些内容会随请求发给模型；`build` 模式下还会写文件、执行命令。作用范围完全取决于你把工作目录设成了哪里，详见 [SECURITY.md](SECURITY.md)。

---

## 五、如何清除数据

1. **清空长期记忆**：退出蛋蛋，删掉后端目录下的 `memory.db`，下次启动会自动重建一个空库。
2. **清空当前对话上下文**：用聊天窗的清空按钮，或直接重启后端（历史只在内存里）。
3. **移除 API Key**：在设置窗把 Key 清空并保存；或直接编辑 `config.yaml`，把两家的 `api_key` 都改回 `""`。**别忘了同时清理 `%USERPROFILE%\.config\opencode\opencode.json` 里的那一份副本。** 如果这个 Key 已经泄露过，请到服务商后台直接吊销它。
4. **清日志**：删掉 `%APPDATA%\dandan-pet\dandan.log`。
5. **彻底卸载**：卸载程序后手动删除 `%APPDATA%\dandan-pet` 目录；opencode 的配置文件属于 opencode 自己的目录，需要你另行清理。

---

## 六、给二次开发者

`backend/config.yaml` 已被 `.gitignore` 排除，仓库里只有空 Key 模板 `backend/config.example.yaml`。fork 或改造本项目时，请不要把自己的 `config.yaml` 提交上去，详细约定见 [SECURITY.md](SECURITY.md)。

隐私方面的疑问或发现的问题，欢迎通过 GitHub Issues 反馈。
